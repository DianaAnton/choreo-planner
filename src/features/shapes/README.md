# features/shapes

Lands in the phase noted in [docs/plan.md](../../../docs/plan.md).
Expose this feature's public API from `index.ts` — other features import from
there and never reach into internals.
